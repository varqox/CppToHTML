#include <iostream>
#include <vector>
#include <stdio.h>

#define trol(a,b)\
cout << a << b

using namespace std;
typedef long double lli;

class troll
{
  vector<int> lol;
};

void trol()
{
  return;
}

int main()
{
	trol(0,1);
    lli trol=0.0;
    std::cout << trol <<"\nlol";
    if(trol==0LL)
    for(int i=0; i<1; ++i)
        if(i==0) break;
    printf("\t%d\n", -1);
return 0;
}