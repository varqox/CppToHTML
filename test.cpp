#include <iostream>
#include <vector>
#include <stdio.h>

#define trol(a,b)\
cout << a << b

using namespace std;
typedef long double lli;

class troll
{
  vector<int> lol;
};

void trol() /*
multiline
comment
:D
*/
{
  return;
}

int main() // oneline comment
{
    trol(0L, 111117162412LL);
    lli trol=0.0;
    std::cout << trol <<"\nlol";
    if(trol==0LL)
    for(int i=0L; i<1; ++i)
        if(i==0) break;
    printf("\t%d\n%c", -1, '\n');
return 0;
}